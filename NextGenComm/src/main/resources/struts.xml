<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
        "http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>

    <!-- 开发者模式 -->
    <constant name="struts.devMode" value="true"/>
    <!-- 设定支持动态方法调用，默认为false -->
    <constant name="struts.enable.DynamicMethodInvocation" value="true"/>
    <!-- 设置最大上传文件为 10 MB -->
    <constant name="struts.multipart.maxSize" value="10485760"/>
    <!-- 默认主题 -->
    <constant name="struts.ui.theme" value="simple"/>
    <!-- 将Action交给Spring管理 -->
    <constant name="struts.objectFactory" value="spring"/>
    <!-- Struts处理的扩展名 -->
    <constant name="struts.action.extension" value="do"/>

    <package name="default" extends="json-default">

        <global-results>
            <result name="login" type="redirect">login.do</result>
            <result name="admin" type="redirect">admin.do</result>
        </global-results>

        <action name="rememberMeLogin" class="remLoginAction">
            <result>login.do</result>
        </action>

        <action name="login">
            <result>/WEB-INF/jsp/login.jsp</result>
        </action>

        <action name="doLogin" class="loginAction">
            <result name="input">login.do</result>
        </action>

        <action name="logout" class="logoutAction">
            <result>login.do</result>
        </action>

        <action name="admin">
            <result>/WEB-INF/jsp/admin/home.jsp</result>
        </action>

    </package>

    <package name="admin" namespace="/admin" extends="json-default">

        <action name="community">
            <result>/WEB-INF/jsp/admin/comm.jsp</result>
        </action>

        <action name="listComm" class="communityAction" method="list">
            <result type="json">
                <param name="root">data</param>
                <param name="excludeNullProperties">true</param>
            </result>
        </action>

        <action name="addComm" class="communityAction" method="add">
            <result type="redirect">community.do</result>
        </action>

        <action name="delComm" class="communityAction" method="delete">
            <result type="redirect">community.do</result>
        </action>

        <action name="building" class="buildingAction">
            <result>/WEB-INF/jsp/admin/build.jsp</result>
        </action>

        <action name="listBuild" class="buildingAction" method="list">
            <result type="json">
                <param name="root">data</param>
                <param name="excludeNullProperties">true</param>
            </result>
        </action>

        <action name="addBuild" class="buildingAction" method="add">
            <result type="redirect">building.do</result>
        </action>

        <action name="addBuildBatch" class="buildingAction" method="addBatch">
            <result type="redirect">building.do</result>
        </action>

        <action name="delBuild" class="buildingAction" method="delete">
            <result type="redirect">building.do</result>
        </action>

    </package>

</struts>